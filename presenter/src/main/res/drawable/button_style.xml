<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Ripple 색상 -->

    <!-- 기본 상태의 버튼 배경 -->
    <item>
        <shape android:shape="rectangle">
            <!-- 파란색 배경 -->
            <solid android:color="#224488" />

            <!-- 흰색 테두리 -->
            <stroke
                android:width="10dp"
                android:color="#4488BB" />

            <!-- 모서리 둥글게 설정 -->
            <corners android:radius="1000dp" />
        </shape>
    </item>
</ripple>